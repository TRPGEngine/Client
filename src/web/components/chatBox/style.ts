import styled from 'styled-components';

export const ChatBoxBtn = styled.div`
  width: 24px;
  height: 24px;
  background-color: ${({ theme }) => theme.color.transparent90};
  border-radius: 50%;
  color: white;
  line-height: 24px;
  text-align: center;
  cursor: pointer;
`;
