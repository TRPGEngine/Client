import { buildRegList } from '@shared/manager/buildRegList';

export const [personalPanelItemList, regPersonalPanelItem] = buildRegList();
export const [personalPanelContentList, regPersonalPanelContent] =
  buildRegList();
