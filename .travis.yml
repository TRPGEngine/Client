sudo: required
os:
- linux
- osx
dist: trusty
osx_image: xcode8
language: node_js
node_js:
  - '7'
  - '8'
  - '9'
matrix:
  allow_failures:
  - node_js: '9'
cache:
  - apt: true
  - directories:
    - "node_modules"
    - "$HOME/.electron"
before_install: build/ci/before_install.sh
install:
  - npm install
  - npm update
script:
  - NODE_ENV=production npm run build
  - npm run package-all
after_success:
  - cd ./dist/
  - cp ../build/CNAME ./
  - mkdir -p src/assets
  - cp -r ../src/assets/* ./src/assets
  - git init
  - git config user.name "travis"
  - git config user.email "travis@travis.com"
  - git add .
  - git commit -m "update file"
  - git branch -m gh-pages
  - git push --force "https://${GH_TOKEN}@${GH_REF}" gh-pages:gh-pages
env:
  global:
    - GH_REF: github.com/TRPGEngine/Client.git
